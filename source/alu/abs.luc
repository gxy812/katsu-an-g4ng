module abs #(SIZE = 32 : SIZE > 2)(
    input in[SIZE],
    output out[SIZE]
) {
    sig n
    always {
        n = in[-1] //msb of in
        out = (in ^ SIZEx{n}) + n // if negative number signalled by 1 in msb, then xor all bits of in and add
        // 1
    }
}